import React from 'react';

const Blog = () => {
    return (
        <div className='container mt-5'>
        <h1>1. What is the difference between Block Element, and Inline-block Element?</h1>

<p>Ans: 


Block Element: Block elements who behaves like blocks.
These elements always start in a new line and occupy the full width of the parent etc. Block elements contain both inline elements and block elements.

Inline-block Element: It is formatted like the inline element where it doesn’t start on a new line. In the inline-block element, we can set the width and height of the properties.</p>

<h1>2. What are semantic tags in HTML?</h1>
    
<p>Semantics means studying the meaning of words and phrases in language here semantics elements is an element with a meaning. 
    <br></br>Non-Semantic Element tells nothing about its content. Those tags are not explaining what to be going to insert here. <br></br>
    Some semantic Elements are those are clearly defines the content. Those tags explain what to be going to insert inside the tags and display the message.

</p>

        </div>
    );
};

export default Blog;